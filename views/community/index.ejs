<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <% include ../partials/head.ejs %>
    </head>
    <body>
        <% include ../partials/header.ejs %>
        <% include ../partials/tab4.ejs %>
        <h1>Community Index Page</h1>
        <!-- <div class="centered">
            <% for(let i = 0; i < community.length; i++){ %>
            <section class="cards">
                <article class="card">
                   <p>content for card one</p>
                   <h5 class="card-title">
                       <%=community[i].name%></h5>
                   <img class="card-img-top" src="<%=community[i].image%>" alt="<%=community[i].name%>'s image">
                   <p class="card-text"><%=community[i].description%>.</p>
                </article>
            </section>
            <% } %>
        </div> -->
        <div>
            <% for(let i = 0; i < community.length; i++){ %>
                <div class="allCommunityCards">
                    <div class="card" >
                        <!-- <div class="col s12 m4"> -->
                        <div class="cardCommunityImage">
                            <img class="card-img-top" src="<%=community[i].image%>" alt="<%=community[i].name%>'s image">
                            <div class="cardCommunityBody">
                                <div id="text" class="caption text-center">
                                    <h5 class="card-title"><a href="/community/<%=community[i].id%>">
                                        <%=community[i].name%></h5>
                                    <p class="cardCommunityText"><%=community[i].description%>.</p>
                                    <% if(username){ %>
                                        <form action="/community/<%=community[i].id%>?_method=DELETE" method="POST">
                                            <input type="submit" value="DELETE"/>
                                        </form>
                                        <a href="/community/<%=community[i].id%>/edit">Edit</a>
                                    <% } %>

                                <!-- </div> -->
                                </div>

                            </div>
                        </div>
                    </div>

            <% } %>
        </div>
    </div>
        <nav>
            <a href="/community/new">Create a new post. </a>
        </nav>

    </body>
</html>
