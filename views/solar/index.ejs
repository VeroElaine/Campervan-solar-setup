solar<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Solar Index Page</h1>
        <% if(username){ %>
            Welcome, <%=username%>
        <% } %>
        <ul>
            <% for(let i = 0; i < solar.length; i++){ %>
                <li>
                    The
                    <a href="/solar/<%=solar[i].id%>">
                        <%=solar[i].applianceName%>
                    </a>
                    is <%=solar[i].watts%>.
                    <% if(solar.watts === "") { %>
                        <%=solar.watts%> = <%=solar.voltage%> x <%=solar.amps%>
                    <% } else { %>
                        <%=solar.watts%>
                    <%}%>
                    <form action="/solar/<%=solar[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                    <a href="/solar/<%=solar[i].id%>/edit">Edit</a>
                </li>
            <% } %>
        </ul>
        <nav>
            <a href="/solar/new">Create a new Solar</a>
        </nav>
    </body>
</html>
